[package]
name = "rust-make"
version = "0.1.0"
edition = "2024"
description = "A GNU Make-compatible build system driver written in Rust"
license = "MIT"

[[bin]]
name = "make"
path = "src/main.rs"

[dependencies]
glob = "0.3"
regex = "1"

[profile.release]
lto = true
strip = true
